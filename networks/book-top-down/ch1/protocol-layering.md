# Protocol Layering

Consists in a model that make complex problems such as the internet easier to work with and understand. In where we can think of each service in the stack as a separate concern that depends on the layer beneath it to achieve its goal.

Layers can be changed, if they maintain the same interface for the adjacent layers.
